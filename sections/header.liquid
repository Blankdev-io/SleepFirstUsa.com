{% liquid 
    assign custom_url_1 = section.settings.custom_url_1
    assign custom_url_2 = section.settings.custom_url_2 
    assign logo_url = section.settings.logo_url
    assign custom_text_1 = section.settings.custom_text_1
    assign custom_text_2 = section.settings.custom_text_2
    assign button_label = section.settings.button_label
    assign button_url = section.settings.button_url
    assign nav_menu = linklists[section.settings.nav_menu.handle]
    assign bar_menu = linklists[section.settings.bar_menu.handle]
    assign banner = section.settings.banner
    assign banner_mobile = section.settings.banner_mobile
    assign banner_url = section.settings.banner_url
    assign banner_1 = section.settings.banner_1
    assign banner_1_url = section.settings.banner_1_url
    assign banner_2 = section.settings.banner_2
    assign banner_2_url = section.settings.banner_2_url
%}

<div id="header">
    <div id="mobile-header"class="text-text bg-white shadow-sp pt-2 pb-4 mb-5 ds:hidden">
        <div class="mx-5">
            <div class="flex justify-around mb-4">
                <a class="flex items-center gap-2" href="{{ custom_url_1 }}">
                    <div class="flex items-center h-6">
                        {% render 'icons', icon: 'map-small' %}
                    </div>
                    <p class="text-xs text-primary font-semibold mm:text-base">{{ custom_text_1 }}</p>
                </a>
                <a class="flex items-center gap-2" href="{{ custom_url_2 }}">
                    <div class="flex items-center h-6">
                        {% render 'icons', icon: 'phone' %}
                    </div>
                    <p class="text-xs text-primary font-semibold mm:text-base">{{ custom_text_2 }}</p>
                </a>
            </div>
            <div class="flex justify-between items-center text mb-4">
                <div class="w-1/6 h-6">
                    <button onclick="myFunctionMenu('myDropdown_mob')" class="block">{% render 'icons', icon: 'menu' %}</button>
                    <div id="ext_div" onclick="myFunctionMenu('myDropdown_mob')" class="block fixed -left-0 top-0 z-40 transition-all duration-300 ease-in-out invisible opacity-0 w-screen h-screen backdrop-blur-[2px] bg-gray-600/50"></div>
                    <div id="myDropdown_mob" class="block fixed -left-0 top-0 z-50 transition-all duration-300 ease-in-out invisible opacity-0 w-max h-screen">
                        <div id="int_div" class="flex flex-col absolute -left-[480px] overflow-auto transition-all duration-600 ease-in-out bg-white rounded-br-cl w-screen max-w-screen-mm h-screen m-auto">
                            {% if nav_menu != blank %}
                                <div class="flex justify-center items-center grow-0 relative py-4 mx-4 border-b border-b-secondary">
                                    <button onclick="myFunctionMenu('myDropdown_mob')" class="absolute left-0 top-5">{% render 'icons', icon: 'big-delete' %}</button>
                                    <a href="{{ logo_url }}">{% render 'icons', icon: 'logo' %}</a>
                                </div>
                                <ul class="flex flex-col gap-4 grow p-4">
                                    {% for link in nav_menu.links %}
                                        <li>
                                            <div class="flex items-center gap-2 text-xl w-full">
                                                <a {% if link.url == '#' %}onclick="myFunctionGrid('{{ link.handle }}{{ forloop.index }}')"{% endif %} href="{{ link.url }}" title="{{ link.title }}" class="grow-0 flex items-center gap-3">
                                                    {% if link.title contains "Discounts" %}
                                                        {% render 'icons', icon: 'nav-discounts' %}{{ link.title | remove: "image" }}
                                                    {% elsif link.title contains "Mattresses" %}
                                                        {% render 'icons', icon: 'nav-mattresses' %}{{ link.title | remove: "image" }}
                                                    {% elsif link.title contains "Chairs" %}
                                                        {% render 'icons', icon: 'nav-chairs' %}{{ link.title | remove: "image" }}
                                                    {% elsif link.title contains "Bases" %}
                                                        {% render 'icons', icon: 'nav-bases' %}{{ link.title | remove: "image" }}
                                                    {% elsif link.title contains "Bedding" %}
                                                        {% render 'icons', icon: 'nav-bedding' %}{{ link.title | remove: "image" }}
                                                    {% else %}
                                                        {% render 'icons', icon: 'map-small' %}{{ link.title | remove: "image" }}
                                                    {% endif %}
                                                </a>
                                                {% if link.links.size > 0 %}
                                                    {% if link.title contains 'image' %}
                                                        <div onclick="myFunctionGrid('{{ link.handle }}{{ forloop.index }}')" class="flex justify-end grow">
                                                            <span  class="-rotate-90">{% render 'icons', icon: 'blue-down' %}</span>
                                                        </div> 
                                                    {% else %}
                                                        <div onclick="myFunctionGrid('{{ link.handle }}{{ forloop.index }}')" class="flex justify-end items-center grow">
                                                            <span class="-rotate-90">{% render 'icons', icon: 'blue-down' %}</span>
                                                        </div>  
                                                    {% endif %}
                                                {% endif %}
                                            </div>
                                            {% if link.links.size > 0 %}
                                                {% if link.title contains 'image' %}
                                                    <div id="{{ link.handle }}{{ forloop.index }}" class="flex flex-col fixed top-0 -left-[480px] z-[60] overflow-auto transition-all duration-600 ease-in-out invisible bg-white rounded-br-cl w-screen max-w-screen-mm h-screen m-auto">
                                                        <div class="flex justify-center items-center grow-0 relative py-4 mx-4 border-b border-b-secondary">
                                                            <button onclick="myFunctionGrid('{{ link.handle }}{{ forloop.index }}')" class="absolute left-0 top-6 rotate-90">{% render 'icons', icon: 'big-blue-down' %}</button>
                                                            <a {% if link.url == '#' %}onclick="myFunctionGrid('{{ link.handle }}{{ forloop.index }}')"{% endif %} href="{{ link.url }}" class="font-bold text-2xl text-center w-4/5">{{ link.title | remove: 'image' }}</a>
                                                        </div>
                                                        <ul class="flex flex-col gap-4 grow p-4">
                                                            {% for sublink in link.links %}
                                                                {% if sublink.links.size == 0 and sublink.type == 'collection_link' %}
                                                                    <li>
                                                                        <div class="flex items-center gap-2 bg-white shadow-mp rounded-cl w-max max-w-full px-2 py-2 mm:gap-4 mm:w-3/4 mm:max-w-none mm:px-5">
                                                                            <a href="{{ sublink.url }}" title="{{ sublink.title }}"><img src="{{ sublink.object.featured_image | image_url: width: 200 }}" alt="{{ sublink.object.featured_image.alt }}" class="object-cover object-center aspect-square rounded-cl w-[120px]" loading="lazy"></a>
                                                                            <div class="flex flex-col justify-evenly gap-2">
                                                                                <a href="{{ sublink.url }}" title="{{ sublink.title }}" class="self-center font-bold text-2xl mb-3">{{ sublink.title }}</a>
                                                                                <a href="{{ sublink.url }}" title="{{ sublink.title }}" class="button text-sm">Shop {{ sublink.title }}</a>
                                                                            </div>
                                                                        </div>
                                                                    </li>
                                                                {% else %}
                                                                    {% if sublink.links.size == 0 %}
                                                                        <li><a href="{{ sublink.url }}" title="{{ sublink.url }}" class="text-xl">{{ sublink.title }}</a></li>
                                                                    {% else %}
                                                                        <li>
                                                                            <div class="flex items-center justify-between gap-2 text-xl w-full">
                                                                                <a {% if sublink.url == '#' %}onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')"{% endif %} href="{{ sublink.url }}" title="{{ sublink.url }}" class="grow-0">{{ sublink.title }}</a>
                                                                                <div onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')" class="flex justify-end items-center grow"><span class="-rotate-90">{% render 'icons', icon: 'blue-down' %}</span></div>
                                                                            </div>
                                                                            <div id="{{ sublink.handle }}{{ forloop.index }}" class="flex flex-col fixed top-0 -left-[480px] z-[70] overflow-auto transition-all duration-600 ease-in-out invisible bg-white rounded-br-cl w-screen max-w-screen-mm h-screen m-auto">
                                                                                <div class="flex justify-center items-center grow-0 relative py-4 mx-4 border-b border-b-secondary">
                                                                                    <button onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')" class="absolute left-0 top-6 rotate-90">{% render 'icons', icon: 'big-blue-down' %}</button>
                                                                                    <a {% if sublink.url == '#' %}onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')"{% endif %} href="{{ sublink.url }}" class="font-bold text-2xl text-center w-4/5">{{ sublink.title }}</a>
                                                                                </div>
                                                                                <ul class="flex flex-col gap-4 grow p-4">
                                                                                {% for item in sublink.links %}
                                                                                    <li><a href="{{ item.url }}" title="{{ item.title }}">{{ item.title }}</a></li>                  
                                                                                {% endfor %}
                                                                                </ul>
                                                                            </div>
                                                                        </li>
                                                                    {% endif %}
                                                                {% endif %}
                                                            {% endfor %}
                                                        </ul>
                                                    </div>
                                                {% elsif link.title contains 'Mattresses' %}
                                                    <div id="{{ link.handle }}{{ forloop.index }}" class="flex flex-col fixed top-0 -left-[480px] z-[60] overflow-auto transition-all duration-600 ease-in-out invisible bg-white rounded-br-cl w-screen max-w-screen-mm h-screen m-auto">
                                                        <div class="flex justify-center items-center grow-0 relative py-4 mx-4 border-b border-b-secondary">
                                                            <button onclick="myFunctionGrid('{{ link.handle }}{{ forloop.index }}')" class="absolute left-0 top-6 rotate-90">{% render 'icons', icon: 'big-blue-down' %}</button>
                                                            <a {% if link.url == '#' %}onclick="myFunctionGrid('{{ link.handle }}{{ forloop.index }}')"{% endif %} href="{{ link.url }}" class="font-bold text-2xl text-center w-4/5">{{ link.title }}</a>
                                                        </div>
                                                        <ul class="flex flex-col gap-4 grow p-4">
                                                            {% for sublink in link.links %}
                                                                {% if sublink.links.size == 0 %}
                                                                    <li><a href="{{ sublink.url }}" title="{{ sublink.title }}" class="text-xl">{{ sublink.title }}</a></li>
                                                                {% else %}
                                                                    <li>
                                                                        <div class="flex items-center justify-between gap-2 text-xl w-full">
                                                                            <a {% if sublink.url == '#' %}onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')"{% endif %} href="{{ sublink.url }}" title="{{ sublink.url }}" class="grow-0">{{ sublink.title }}</a>
                                                                            <div onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')" class="flex justify-end items-center grow"><span class="-rotate-90">{% render 'icons', icon: 'blue-down' %}</span></div>
                                                                        </div>
                                                                        <div id="{{ sublink.handle }}{{ forloop.index }}" class="flex flex-col fixed top-0 -left-[480px] z-[70] overflow-auto transition-all duration-600 ease-in-out invisible bg-white rounded-br-cl w-screen max-w-screen-mm h-screen m-auto">
                                                                            <div class="flex justify-center items-center grow-0 relative py-4 mx-4 border-b border-b-secondary">
                                                                                <button onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')" class="absolute left-0 top-6 rotate-90">{% render 'icons', icon: 'big-blue-down' %}</button>
                                                                                <a {% if sublink.url == '#' %}onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')"{% endif %} href="{{ sublink.url }}" class="font-bold text-2xl text-center w-4/5">{{ sublink.title }}</a>
                                                                            </div>
                                                                            <ul class="flex flex-col gap-4 grow p-4">
                                                                            {% for item in sublink.links %}
                                                                                <li><a href="{{ item.url }}" title="{{ item.title }}">{{ item.title }}</a></li>                   
                                                                            {% endfor %}
                                                                            </ul>
                                                                        </div>
                                                                    </li>
                                                                {% endif %}
                                                            {% endfor %}
                                                        </ul>
                                                        <div class="grid grid-rows-2 gap-4 p-4">
                                                            <a href="{{ banner_1_url }}" title="{{ banner_1_url.title }}"><picture>
                                                                <img class="object-cover rounded-cl w-full" src="{{ banner_1 | image_url: width: 800 }}" alt="{{ banner_1.alt }}" title="{{ banner_1.title }}">
                                                            </picture></a>
                                                            <a href="{{ banner_2_url }}" title="{{ banner_2_url.title }}"><picture>
                                                                <img class="object-cover rounded-cl w-full" src="{{ banner_2 | image_url: width: 800 }}" alt="{{ banner_2.alt }}" title="{{ banner_2.title }}">
                                                            </picture></a>
                                                        </div>
                                                    </div>
                                                {% else %}
                                                    <div id="{{ link.handle }}{{ forloop.index }}" class="flex flex-col fixed top-0 -left-[480px] z-[60] overflow-auto transition-all duration-600 ease-in-out invisible bg-white rounded-br-cl w-screen max-w-screen-mm h-screen m-auto">
                                                        <div class="flex justify-center items-center grow-0 relative py-4 mx-4 border-b border-b-secondary">
                                                            <button onclick="myFunctionGrid('{{ link.handle }}{{ forloop.index }}')" class="absolute left-0 top-6 rotate-90">{% render 'icons', icon: 'big-blue-down' %}</button>
                                                            <a {% if link.url == '#' %}onclick="myFunctionGrid('{{ link.handle }}{{ forloop.index }}')"{% endif %} href="{{ link.url }}" class="font-bold text-2xl text-center w-4/5">{{ link.title }}</a>
                                                        </div>
                                                        <ul class="flex flex-col gap-4 grow p-4">
                                                            {% for sublink in link.links %}
                                                                {% if sublink.links.size == 0 %}
                                                                    <li><a href="{{ sublink.url }}" title="{{ sublink.title }}" class="text-xl">{{ sublink.title }}</a></li>
                                                                {% else %}
                                                                    <li>
                                                                        <div class="flex items-center justify-between gap-2 text-xl w-full">
                                                                            <a {% if sublink.url == '#' %}onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')"{% endif %} href="{{ sublink.url }}" title="{{ sublink.url }}" class="grow-0">{{ sublink.title }}</a>
                                                                            <div onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')" class="flex justify-end items-center grow"><span class="-rotate-90">{% render 'icons', icon: 'blue-down' %}</span></div>
                                                                        </div>
                                                                        <div id="{{ sublink.handle }}{{ forloop.index }}" class="flex flex-col fixed top-0 -left-[480px] z-[70] overflow-auto transition-all duration-600 ease-in-out invisible bg-white rounded-br-cl w-screen max-w-screen-mm h-screen m-auto">
                                                                            <div class="flex justify-center items-center grow-0 relative py-4 mx-4 border-b border-b-secondary">
                                                                                <button onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')" class="absolute left-0 top-6 rotate-90">{% render 'icons', icon: 'big-blue-down' %}</button>
                                                                                <a {% if sublink.url == '#' %}onclick="myFunctionGrid('{{ sublink.handle }}{{ forloop.index }}')"{% endif %} href="{{ sublink.url }}" class="font-bold text-2xl text-center w-4/5">{{ sublink.title }}</a>
                                                                            </div>
                                                                            <ul class="flex flex-col gap-4 grow p-4">
                                                                            {% for item in sublink.links %}
                                                                                <li><a href="{{ item.url }}" title="{{ item.title }}">{{ item.title }}</a></li>                   
                                                                            {% endfor %}
                                                                            </ul>
                                                                        </div>
                                                                    </li>
                                                                {% endif %}
                                                            {% endfor %}
                                                        </ul>
                                                    </div>
                                                {% endif %}
                                            {% endif %}
                                        </li>                    
                                    {% endfor %}
                                </ul>
                                {% if bar_menu != blank %}
                                    <ul class="flex flex-wrap gap-2 m-4">
                                        {% for link in bar_menu.links %}
                                            {% if link.title == "15% OFF VIP" %}
                                                <li>
                                                    <button title="{{ link.title }}" class="button-white cursor-pointer klaviyo_form_trigger1">{{ link.title }}</button>
                                                </li>
                                            {% else %}
                                                <li>
                                                    <a href="{{ link.url }}" title="{{ link.title }}" class="button-white">{{ link.title }}</a>
                                                </li>
                                            {% endif %}
                                        {% endfor %}
                                    </ul>
                                {% endif %}
                                <div>
                                    <a href="{{ button_url }}" title="{{ button_url.title }}" class="button-accent w-max m-4">{{ button_label }}</a>
                                </div>
                                <div class="text-text appearance-none border border-text rounded-full focus:border-secondary focus:outline-none w-max mx-4 mb-20">
                                    <div class="transcy-switcher-manual transcy-mobile hidden"></div>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
                <a href="{{ logo_url }}">
                    {% render 'icons', icon: 'logo' %}
                </a>
                <div class="flex justify-end gap-1 w-1/6 mm:gap-2">
                    <div onclick="myFunctionSearch('searchdrop')" class="h-6">
                        {% render 'icons', icon: 'search' %}
                    </div>
                    <div id="ext_search" onclick="myFunctionSearch('searchdrop')" class="absolute z-40 top-0 right-0 transition-all duration-300 ease-in-out invisible opacity-0 backdrop-blur-[2px] bg-gray-600/50 w-screen h-screen">
                    </div>
                    <div id="searchdrop" class="flex justify-center absolute z-50 top-0 right-0 transition-all duration-300 ease-in-out invisible opacity-0 w-screen h-max">
                        <div id="main-search-container-mobile" class="relative top-6 w-full max-w-[715px] mx-5 dm:mx-auto ds:mx-9">
                            <div class="flex gap-1">
                                <div onclick="myFunctionSearch('searchdrop')" title="Close" class="cursor-pointer bg-primary shadow-sp rounded-full p-2">{% render 'icons', icon: 'white-delete' %}</div>
                                <input v-model="searchTerm" @keyup="searchBar()" @keyup.enter="searchPage()" id="search-bar-mobile" class="outline-none border-none rounded-full focus:border-secondary focus:outline-none w-full py-[5px] pl-4 pr-8" type="text" placeholder="Type to search" autocomplete="off">
                                <button @click="searchPage()" class="flex items-center absolute top-1 right-0 pr-4" title="search"> 
                                    {% render 'icons', icon: 'search' %}
                                </button>
                            </div>
                            <template v-if="showSearch == true && searchTerm != '' && resultsCount != 0">
                                <div id="search-container-mobile" class="flex flex-col gap-2 absolute z-10 top-12 bg-white rounded-cl shadow-sp p-3 mm:p-4">
                                    <template v-if="products.length > 0 && products[0] != undefined">
                                        <div id="productsResults">
                                            <h3 class="font-bold text-center mb-2">Products</h3>
                                            <div class="grid grid-cols-2 gap-2">
                                                <template v-for="(product, index) in products">
                                                <a class="flex items-center gap-2" :href="product.url" :title="product.title">
                                                    <img class="object-cover rounded-cs w-16 h-16" :src="product.featured_image.url" :alt="product.featured_image.alt" loading="lazy">
                                                    <div class="flex flex-col">
                                                        <p class="line-clamp-2 font-semibold text-xs">${ product.title }</p>
                                                        <p class="text-xs">${ product.vendor }</p>
                                                        <p class="text-xs"><span>${ product.price }</span></p>
                                                    </div>   
                                                </a>
                                                </template>
                                            </div>
                                        </div>
                                    </template>
                                    <template v-if="articles.length > 0 && articles[0] != undefined">
                                        <div id="articlesResults">
                                            <h3 class="font-bold text-center mb-2">Articles</h3>
                                            <div class="grid grid-cols-2 gap-2">
                                                <template v-for="(article, index) in articles">
                                                <a class="flex items-center gap-2" :href="article.url" :title="article.title">
                                                    <img class="object-cover rounded-cs w-16 h-16" :src="article.featured_image.url" :alt="article.featured_image.alt" loading="lazy">
                                                    <p class="line-clamp-3 font-semibold text-xs">${ article.title }</p> 
                                                </a>
                                                </template>
                                            </div>
                                        </div>
                                    </template>
                                    <template v-if="collections.length > 0 && collections[0] != undefined">
                                        <div id="collectionsResults">
                                            <h3 class="font-bold text-center mb-2">Collections</h3>
                                            <div class="flex flex-wrap justify-center gap-2">
                                                <template v-for="(collection, index) in collections">
                                                <a class="font-semibold text-xs text-white bg-primary rounded-full py-1 px-3" :href="collection.url" :title="collection.title">${ collection.title }</a>
                                                </template>
                                            </div>
                                        </div>
                                    </template>
                                    <template v-if="pages.length > 0 && pages[0] != undefined">
                                        <div id="pagesResults">
                                            <h3 class="font-bold text-center mb-2">Pages</h3>
                                            <div class="flex flex-wrap justify-center gap-2">
                                                <template v-for="(page, index) in pages">
                                                <a class="font-semibold text-xs text-white bg-primary rounded-full py-1 px-3" :href="page.url" :title="page.title">${ page.title }</a>
                                                </template>
                                            </div>
                                        </div>
                                    </template>
                                </div>
                            </template>
                            <template v-else-if="showSearch == true && searchTerm != '' && resultsCount != 0 &&  collections[0].title == 'Flash Sale'">
                                <div id="search-container-mobile" class="absolute z-10 left-9 top-12 bg-white rounded-cl shadow-sp p-4">
                                    <p>Sorry, nothing found for "${ searchTerm }".</p>
                                </div>
                            </template>
                            <template v-else-if="showSearch == true && searchTerm != '' && resultsCount == 0">
                                <div id="search-container-mobile" class="absolute z-10 left-9 top-12 bg-white rounded-cl shadow-sp p-4">
                                    <p>Sorry, nothing found for "${ searchTerm }".</p>
                                </div>
                            </template>
                            <template v-else-if="showSearch == true && searchTerm == ''">
                                <div id="search-container-mobile" class="absolute z-10 left-9 top-12 bg-white rounded-cl shadow-sp p-4">
                                    <p>Type to search Products, Articles, Collections and pages.</p>
                                </div>
                            </template>
                        </div>
                    </div>
                    <a class="relative h-6" href="/cart">
                        <span id="cart-counter-mb" class="absolute -top-2 -right-1 bg-secondary rounded-full py-0.5 px-1.5 text-xs text-white">${ cartCount }</span>
                        {% render 'icons', icon: 'cart' %}
                    </a>
                </div>
            </div>
            {% if template == 'index' %}
                <a id="top_banner_mobile" href="{{ banner_url }}" alt="{{ banner_url.alt }}" title="{{ banner_url.title }}" class="block transition-all duration-300 ease-in-out"><picture>
                    <img class="rounded-cl w-full" src="{{ banner_mobile | image_url }}" alt="{{ banner_mobile.alt }}" title="{{ banner_mobile.title }}">
                </picture></a>
            {% endif %}
        </div>
    </div>
    
    <div id="desk-header" class="hidden z-10 text-text bg-white ds:block">
        <div class="flex justify-between items-center pt-5 pb-4 ds:mx-9 dm:mx-auto dm:max-w-screen-dm dm:px-10 dl:px-4">
            <div> 
                <a href="{{ logo_url }}" title="Sleep First">
                    {% render 'icons', icon: 'logo' %}
                </a>
            </div>
            <div class="flex justify-between gap-1 w-[72%]">
                <div id="main-search-container-background" class="fixed z-10 top-0 right-0 transition-all duration-300 ease-in-out invisible opacity-0 backdrop-blur-[2px] bg-gray-600/50 w-screen h-screen"></div>
                <div id="main-search-container" class="relative w-[55%]">
                    <input v-model="searchTerm" @keyup="searchBar()" @keyup.enter="searchPage()" id="search-bar" class="relative border border-text rounded-full py-1 pl-4 pr-8 focus:z-20 focus:border-secondary focus:outline-none h-full w-full" type="text" placeholder="Type to search" autocomplete="off">
                    <button @click="searchPage()" class="flex items-center absolute inset-y-0 right-0 z-20 pr-4" title="search"> 
                        {% render 'icons', icon: 'search' %}
                    </button>
                    <template v-if="showSearch == true && searchTerm != '' && resultsCount != 0 && collections.length > 0 && collections[0] != undefined && collections[0].title != 'Flash Sale'">
                        <div id="search-container" class="flex flex-col gap-2 absolute z-20 top-12 transition-all duration-300 ease-in-out bg-white rounded-cl shadow-mp p-4" tabindex="0">
                            <template v-if="products.length > 0 && products[0] != undefined">
                                <div id="productsResults">
                                    <h3 class="font-bold text-center mb-2">Products</h3>
                                    <div class="grid grid-cols-2 gap-2">
                                        <template v-for="(product, index) in products">
                                        <a class="flex items-center gap-2" :href="product.url" :title="product.title">
                                            <img class="object-cover rounded-cs w-16 h-16" :src="product.featured_image.url" :alt="product.featured_image.alt" loading="lazy">
                                            <div class="flex flex-col">
                                                <p class="line-clamp-2 font-semibold text-xs">${ product.title }</p>
                                                <p class="text-xs">${ product.vendor }</p>
                                                <p class="text-xs"><span>${ product.price }</span></p>
                                            </div>   
                                        </a>
                                        </template>
                                    </div>
                                </div>
                            </template>
                            <template v-if="articles.length > 0 && articles[0] != undefined">
                                <div id="articlesResults">
                                    <h3 class="font-bold text-center mb-2">Articles</h3>
                                    <div class="grid grid-cols-2 gap-2">
                                        <template v-for="(article, index) in articles">
                                        <a class="flex items-center gap-2" :href="article.url" :title="article.title">
                                            <img class="object-cover rounded-cs w-16 h-16" :src="article.featured_image.url" :alt="article.featured_image.alt" loading="lazy">
                                            <p class="line-clamp-3 font-semibold text-xs">${ article.title }</p> 
                                        </a>
                                        </template>
                                    </div>
                                </div>
                            </template>
                            <template v-if="collections.length > 0 && collections[0] != undefined && collections[0].title != 'Flash Sale'">
                                <div id="collectionsResults">
                                    <h3 class="font-bold text-center mb-2">Collections</h3>
                                    <div class="flex flex-wrap justify-center gap-2">
                                        <template v-for="(collection, index) in collections">
                                        <a v-if="collection.title != 'Flash Sale'" class="font-semibold text-xs text-white bg-primary rounded-full py-1 px-3" :href="collection.url" :title="collection.title">${ collection.title }</a>
                                        </template>
                                    </div>
                                </div>
                            </template>
                            <template v-if="pages.length > 0 && pages[0] != undefined">
                                <div id="pagesResults">
                                    <h3 class="font-bold text-center mb-2">Pages</h3>
                                    <div class="flex flex-wrap justify-center gap-2">
                                        <template v-for="(page, index) in pages">
                                        <a class="font-semibold text-xs text-white bg-primary rounded-full py-1 px-3" :href="page.url" :title="page.title">${ page.title }</a>
                                        </template>
                                    </div>
                                </div>
                            </template>
                        </div>
                    </template>
                    <template v-else-if="showSearch == true && searchTerm != '' && resultsCount != 0 && collections.length > 0 && collections[0] != undefined && collections[0].title == 'Flash Sale'">
                        <div id="search-container" class="absolute z-20 top-12 transition-all duration-300 ease-in-out bg-white rounded-cl shadow-mp p-4">
                            <p>Sorry, nothing found for "${ searchTerm }".</p>
                        </div>
                    </template>
                    <template v-else-if="showSearch == true && searchTerm != '' && resultsCount != 0">
                        <div id="search-container" class="flex flex-col gap-2 absolute z-20 top-12 transition-all duration-300 ease-in-out bg-white rounded-cl shadow-mp p-4" tabindex="0">
                            <template v-if="products.length > 0 && products[0] != undefined">
                                <div id="productsResults">
                                    <h3 class="font-bold text-center mb-2">Products</h3>
                                    <div class="grid grid-cols-2 gap-2">
                                        <template v-for="(product, index) in products">
                                        <a class="flex items-center gap-2" :href="product.url" :title="product.title">
                                            <img class="object-cover rounded-cs w-16 h-16" :src="product.featured_image.url" :alt="product.featured_image.alt" loading="lazy">
                                            <div class="flex flex-col">
                                                <p class="line-clamp-2 font-semibold text-xs">${ product.title }</p>
                                                <p class="text-xs">${ product.vendor }</p>
                                                <p class="text-xs"><span>${ product.price }</span></p>
                                            </div>   
                                        </a>
                                        </template>
                                    </div>
                                </div>
                            </template>
                            <template v-if="articles.length > 0 && articles[0] != undefined">
                                <div id="articlesResults">
                                    <h3 class="font-bold text-center mb-2">Articles</h3>
                                    <div class="grid grid-cols-2 gap-2">
                                        <template v-for="(article, index) in articles">
                                        <a class="flex items-center gap-2" :href="article.url" :title="article.title">
                                            <img class="object-cover rounded-cs w-16 h-16" :src="article.featured_image.url" :alt="article.featured_image.alt" loading="lazy">
                                            <p class="line-clamp-3 font-semibold text-xs">${ article.title }</p> 
                                        </a>
                                        </template>
                                    </div>
                                </div>
                            </template>
                            <template v-if="collections.length > 0 && collections[0] != undefined && collections[0].title != 'Flash Sale'">
                                <div id="collectionsResults">
                                    <h3 class="font-bold text-center mb-2">Collections</h3>
                                    <div class="flex flex-wrap justify-center gap-2">
                                        <template v-for="(collection, index) in collections">
                                        <a v-if="collection.title != 'Flash Sale'" class="font-semibold text-xs text-white bg-primary rounded-full py-1 px-3" :href="collection.url" :title="collection.title">${ collection.title }</a>
                                        </template>
                                    </div>
                                </div>
                            </template>
                            <template v-if="pages.length > 0 && pages[0] != undefined">
                                <div id="pagesResults">
                                    <h3 class="font-bold text-center mb-2">Pages</h3>
                                    <div class="flex flex-wrap justify-center gap-2">
                                        <template v-for="(page, index) in pages">
                                        <a class="font-semibold text-xs text-white bg-primary rounded-full py-1 px-3" :href="page.url" :title="page.title">${ page.title }</a>
                                        </template>
                                    </div>
                                </div>
                            </template>
                        </div>
                    </template>
                    <template v-else-if="showSearch == true && searchTerm != '' && resultsCount == 0">
                        <div id="search-container" class="absolute z-20 top-12 transition-all duration-300 ease-in-out bg-white rounded-cl shadow-mp p-4">
                            <p>Sorry, nothing found for "${ searchTerm }".</p>
                        </div>
                    </template>
                    <template v-else-if="showSearch == true && searchTerm == ''">
                        <div id="search-container" class="absolute z-20 top-12 transition-all duration-300 ease-in-out bg-white rounded-cl shadow-mp p-4">
                            <p>Type to search Products, Articles, Collections and pages.</p>
                        </div>
                    </template>
                </div>
                <div>
                    <a href="{{ button_url }}" title="{{ button_url.title }}" class="button-accent">{{ button_label }}</a>
                </div>
                <div class="">
                    <div class="text-text appearance-none w-full border border-text rounded-full focus:border-secondary focus:outline-none">
                        <div class="transcy-switcher-manual transcy-desktop hidden"></div>
                    </div>
                </div>
            </div>
          
            <div id="deskCart" class="group peer relative">
                <a class="relative block h-6 group-hover:z-50" href="/cart">
                    <span id="cart-counter" class="absolute -top-2 -right-1 bg-secondary rounded-full py-0.5 px-1.5 px text-xs text-white">${ cartCount }</span>
                    {% render 'icons', icon: 'cart' %}
                </a>
                <div class="grid absolute z-40 top-10 -right-[17px] transition-all duration-300 ease-in-out invisible opacity-0 group-hover:visible group-hover:opacity-100 text-text bg-white shadow-lp rounded-cl overflow-auto overscroll-none w-[450px] max-h-cart px-5 py-5 dl:-right-[17px]">
                    <div v-if="cartCount > 0">
                        <a href="/cart" title="Cart" class="font-bold text-xl">Shopping Cart</a>
                        <div v-if="cartProducts.length > 0" v-for="(product, index) in cartProducts" class="grid grid-cols-3 gap-y-5 relative text-sm h-[156px] py-2">
                            <span @click="headerRemoveItem(index)" title="Delete" class="absolute right-0 top-5 cursor-pointer">{% render 'icons', icon: 'delete' %}</span>
                            <div class="flex justify-center items-center">
                                <a :href="product.url">
                                    <img :src="product.featured_image.url + '&width=180'" :alt="product.featured_image.alt" class="object-cover object-center rounded-cl" loading="lazy">
                                </a>
                            </div>
                            <div class="col-span-2 flex flex-col justify-evenly px-3">
                                <a :href="product.url" class="font-semibold text-sm">${ product.product_title}</a>
                                <span class="font-medium text-xs text-accent-2">${ product.variant_title }</span>
                                <div class="relative w-2/4">
                                    <button @click="headerDecrement(index)" class="flex items-center absolute inset-y-0 left-0 pl-2 mm:pl-4" type="button" title="quantity minus" data-id="add-btn">-</button>
                                    <input class="text-center appearance-none border rounded-full border-primary focus:border-secondary focus:outline-none w-full py-0.5 px-4" type="number" name="headquantity" :value="product.quantity" min="1" title="quantity input">
                                    <button @click="headerIncrement(index)" class="flex items-center absolute inset-y-0 right-0 pr-2 mm:pr-4" type="button" title="quantity plus" data-id="add-btn">+</button>
                                </div>
                                <div class="flex flex-col font-medium text-sm">
                                    {% if item.original_line_price != item.line_price %}{{ item.original_line_price | money }}{% endif %}
                                    ${ product.line_price }
                                    <div v-for="discount in product.discounts">
                                    ${ discount.title }
                                    </div>
                                </div>
                            </div>
                        </div>
                        <p class="font-extrabold text-xl text-right mt-4">Subtotal: ${ cartTotalPrice }</p>
                        <a href="/cart" title="Cart" class="button mt-4">CART</a>
                    </div>
                    <div v-else-if="cartCount == 0">
                        <p class="font-semibold text-2xl mb-4">Your cart is empty, but you can take a look at our promotions:</p>
                        <a href="/collections/on-sale" title="Promotions" class="button">Continue Shopping</a>
                    </div>
                </div>
                <div class="absolute z-40 top-8 right-0.5 transition-all duration-300 ease-in-out invisible opacity-0 rotate-45 aspect-square bg-white group-hover:visible group-hover:opacity-100 w-5"></div>
            </div>
            <div class="fixed z-30 top-0 right-0 transition-all duration-300 ease-in-out invisible opacity-0 backdrop-blur-[2px] bg-gray-600/50 peer-hover:visible peer-hover:opacity-100 w-screen h-screen"></div>
        </div>
        {% if template == 'index' %}
            <div class="ds:mx-9 dm:mx-auto dm:max-w-screen-dm dm:px-10 dl:px-4">
                <div id="top_banner" class="w-full">
                    <a href="{{ banner_url }}" alt="{{ banner_url.alt }}" title="{{ banner_url.title }}"><picture>
                        <img class="rounded-cl w-full h-full" src="{{ banner| image_url }}" alt="{{ banner.alt }}" title="{{ banner.title }}">
                    </picture></a>
                </div>
            </div>
        {% endif %}
        <div class="relative p-4 m-auto dm:max-w-screen-dm">
            {% if nav_menu != blank %}
                <ul class="flex justify-evenly">
                    {% for link in nav_menu.links %}
                        <li>
                        <div class="group peer">
                            <a class="flex items-center gap-2 text-xl font-bold text-text" href="{{ link.url }}">
                                {{ link.title | remove: "image" }} 
                                {% if link.links.size > 0 %}
                                    <span class="transition-all duration-300 ease-in-out group-hover:rotate-180">{% render 'icons', icon: 'blue-down' %}</span> 
                                {% endif %}
                            </a>
                            {% if link.links.size > 0 %}
                                {% if link.title contains 'image' %}
                                    <div class="absolute z-20 -left-px font-bold transition-all duration-300 ease-in-out invisible opacity-0 group-hover:visible group-hover:opacity-100 shadow-ln rounded-b-cl overflow-hidden w-[100%]">
                                        <div class="grid grid-cols-5 grid-rows-3 gap-x-10 gap-y-3 bg-white rounded-b-cl py-8 px-20">
                                            {% for sublink in link.links %}
                                                {% if sublink.links.size == 0 and sublink.type == 'collection_link' %}
                                                    <div class="col-span-2 row-span-1 flex items-center justify-evenly gap-2 bg-white shadow-mp rounded-cl w-full my-3 px-5 py-2 mm:gap-4">
                                                        <a href="{{ sublink.url }}" title="{{ sublink.title }}"><img src="{{ sublink.object.featured_image | image_url: width: 200 }}" alt="{{ sublink.object.featured_image.alt }}" class="object-cover object-center aspect-square rounded-cl w-[120px]" loading="lazy"></a>
                                                        <div class="flex flex-col justify-evenly gap-2">
                                                            <a href="{{ sublink.url }}" title="{{ sublink.title }}" class="self-center font-bold text-2xl mb-3">{{ sublink.title }}</a>
                                                            <a href="{{ sublink.url }}" title="{{ sublink.title }}" class="button">Shop {{ sublink.title }}</a>
                                                        </div>
                                                    </div>
                                                {% else %}
                                                    {% if sublink.links.size == 0 %}
                                                        <a href="{{ sublink.url }}" title="{{ sublink.title }}"><p class="text-primary text-xl">{{ sublink.title }}</p></a>
                                                    {% else %}
                                                        <div class="col-span-1 row-span-3 divide-y divide-secondary">
                                                            <a href="{{ sublink.url }}" title="{{ sublink.title }}"><p class="text-primary text-xl">{{ sublink.title }}</p></a>
                                                            <div class="text-text mt-4 pt-4">
                                                                {% for item in sublink.links %}
                                                                    <ul><a href="{{ item.url }}" title="{{ item.title }}">{{ item.title }}</a></ul>
                                                                {% endfor %}
                                                            </div>
                                                        </div>
                                                    {% endif %}
                                                {% endif %}
                                            {% endfor %}
                                        </div> 
                                    </div>
                                {%- elsif link.title contains 'Mattresses' -%}
                                    <div class="absolute z-20 -left-px font-bold transition-all duration-300 ease-in-out invisible opacity-0 group-hover:visible group-hover:opacity-100 shadow-ln rounded-b-cl overflow-hidden w-[100%]">
                                        <div class="bg-white rounded-b-cl py-8 px-20">
                                            <div class="grid grid-cols-5 gap-10 mb-6">
                                                {% for sublink in link.links %}
                                                    {% if sublink.links.size == 0 %}
                                                        <a href="{{ sublink.url }}" title="{{ sublink.title }}"><p class="text-primary text-xl">{{ sublink.title }}</p></a>
                                                    {% else %}
                                                        <div class="divide-y divide-secondary">
                                                            <a href="{{ sublink.url }}" title="{{ sublink.title }}"><p class="text-primary text-xl">{{ sublink.title }}</p></a>
                                                            <div class="text-text mt-4 pt-4">
                                                                {% for item in sublink.links %}
                                                                    <ul><a href="{{ item.url }}" title="{{ item.title }}">{{ item.title }}</a></ul>
                                                                {% endfor %}
                                                            </div>
                                                        </div>
                                                    {% endif %}
                                                {% endfor %}
                                            </div>
                                            <div class="grid grid-cols-2 gap-10">
                                                <a href="{{ banner_1_url }}" title="{{ banner_1_url.title }}"><picture>
                                                    <img class="object-cover rounded-cl w-full h-full" src="{{ banner_1 | image_url: width: 800 }}" alt="{{ banner_1.alt }}" title="{{ banner_1.title }}">
                                                </picture></a>
                                                <a href="{{ banner_2_url }}" title="{{ banner_2_url.title }}"><picture>
                                                    <img class="object-cover rounded-cl w-full h-full" src="{{ banner_2 | image_url: width: 800 }}" alt="{{ banner_2.alt }}" title="{{ banner_2.title }}">
                                                </picture></a>
                                            </div>
                                        </div>
                                    </div>
                                {% else %}
                                    <div class="absolute z-20 -left-px font-bold transition-all duration-300 ease-in-out invisible opacity-0 group-hover:visible group-hover:opacity-100 shadow-ln rounded-b-cl overflow-hidden w-[100%]">
                                        <div class="grid grid-cols-5 gap-10 bg-white rounded-b-cl py-8 px-20">
                                            {% for sublink in link.links %}
                                                {% if sublink.links.size == 0 %}
                                                    <a href="{{ sublink.url }}" title="{{ sublink.title }}"><p class="text-primary text-xl">{{ sublink.title }}</p></a>
                                                {% else %}
                                                    <div class="divide-y divide-secondary">
                                                        <a href="{{ sublink.url }}" title="{{ sublink.title }}"><p class="text-primary text-xl">{{ sublink.title }}</p></a>
                                                        <div class="text-text mt-4 pt-4">
                                                            {% for item in sublink.links %}
                                                                <ul><a href="{{ item.url }}" title="{{ item.title }}">{{ item.title }}</a></ul>
                                                            {% endfor %}
                                                        </div>
                                                    </div>
                                                {% endif %}
                                            {% endfor %}
                                        </div>
                                    </div>
                                {% endif %}
                            {% endif %}
                        </div>
                        {% if link.links.size > 0 %}
                            <div class="fixed -z-10 top-12 right-0 transition-all duration-300 ease-in-out invisible opacity-0 backdrop-blur-[2px] bg-gray-600/50 peer-hover:visible peer-hover:opacity-100 w-screen h-screen"></div>
                        {% endif %}
                        </li>
                    {% endfor %}
                </ul>
            {% endif %}
        </div>
    </div>
</div>


<script>
var { createApp } = Vue;

const appHeader = createApp({
    delimiters: ['${', '}'],
    data() {
        const cartProducts = "",
              cartCount = 0,
              cartTotalPrice = "",
              searchTerm = '',
              showSearch = false,
              resultsCount = 0,
              results = {},
              products = [],
              articles = [],
              collections = [],
              pages = [];
        return {
            cartProducts,
            cartCount,
            cartTotalPrice,
            searchTerm,
            showSearch,
            resultsCount,
            results,
            products,
            articles,
            collections,
            pages
        }
    },
    created() {
        document.addEventListener('click', this.showSearchResults);
        window.addEventListener("load", (event) => {
            const addToCartBtn = document.querySelectorAll('.addToCartBtn');
            for (var i = 0; i < addToCartBtn.length; i++) {
                addToCartBtn[i].addEventListener('click', this.updateCar)
            }
        });

        const options = {
            method: 'GET'
        }
        fetch('/cart.js', options)
            .then(response => {
                if(response.ok) {
                    return response.json();
                } else {
                    throw new Error(response.status);
                }
            })
            .then(data => {
                const cartData = data;
                const formatter = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 2, vmaximumFractionDigits: 2, });
                this.cartProducts = cartData.items;
                this.cartCount = data.item_count;
                this.cartTotalPrice = formatter.format((cartData.total_price/100).toFixed(2));

                const productArray = this.cartProducts;
                productArray.forEach((item, index) => {
                    item.price = formatter.format((item.price/100).toFixed(2));
                    item.original_price = formatter.format((item.original_price/100).toFixed(2));
                    item.discounted_price = formatter.format((item.discounted_price/100).toFixed(2));
                    item.line_price = formatter.format((item.line_price/100).toFixed(2));
                    item.original_line_price = formatter.format((item.original_line_price/100).toFixed(2));
                    item.total_discount = formatter.format((item.total_discount/100).toFixed(2));
                    item.final_price = formatter.format((item.final_price/100).toFixed(2));
                    item.final_line_price = formatter.format((item.final_line_price/100).toFixed(2));
                });
            })
            .catch(error => {
                console.log(error);
            });
    },
    methods: {
        searchPage() {
            const searchQuery = this.searchTerm.replace(/\s+/g, '+');
            window.location.href = `/search?q=${ searchQuery }`
        },
        searchBar() {
            fetch(`/search/suggest.json?q=${this.searchTerm}&resources[type]=product,page,article,collection&resources[limit]=4&resources[options][unavailable_products]=hide&resources[options][fields]=title,product_type,variants.title`)
                .then((response) => {
                    if (response.ok) {
                    return response.json()
                    } else {
                        throw new Error(response.status)
                    }
                })
                .then((suggestions) => {
                    const productSuggestions = suggestions.resources.results;
                    const empty = [];

                    if (productSuggestions.products) {
                        this.products = productSuggestions.products;
                    } else {
                        this.products = empty;
                    }
                    if (productSuggestions.collections) {
                        this.collections = productSuggestions.collections;
                    } else {
                        this.collections = empty;
                    }
                    if (productSuggestions.pages) {
                        this.pages = productSuggestions.pages;
                    } else {
                        this.pages = empty;
                    }
                    if (productSuggestions.articles) {
                        this.articles = productSuggestions.articles;
                    } else {
                        this.articles = empty;
                    }
                    this.resultsCount = this.products.length + this.collections.length + this.pages.length + this.articles.length;

                })
                .catch(error => {
                    console.log(error);
                });
        },
        showSearchResults(event) {
                const mainSearchContainerMobile = document.getElementById('main-search-container-mobile');
                const mainSearchContainer = document.getElementById('main-search-container');
                const background = document.getElementById('main-search-container-background');

                if (mainSearchContainer.contains(event.target) || mainSearchContainerMobile.contains(event.target)) {
                    this.showSearch = true;
                    background.classList.remove('invisible');
                    background.classList.add('visible');
                    background.classList.remove('opacity-0');
                    background.classList.add('opacity-100');
                } else {
                    this.showSearch = false;
                    background.classList.remove('visible');
                    background.classList.add('invisible');
                    background.classList.remove('opacity-100');
                    background.classList.add('opacity-0');
                }
        },
        headerIncrement(index) {
            const headqty = this.cartProducts[index].quantity;
            const headkey = this.cartProducts[index].key;
            const options = {
                method: 'POST',
                headers: { 'content-type': 'application/json' },
                body: JSON.stringify({
                    id: `${headkey}`,
                    quantity: headqty + 1
                })
            };
            fetch('/cart/change.js', options)
                .then( response => {
                    if (response.ok) {
                        return response.json();
                    } else {
                        throw new Error(response.status);
                    }
                })
                .then(data => {
                    
                    const cartData = data;
                    const formatter = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 2, vmaximumFractionDigits: 2, });
                    this.cartProducts = cartData.items;
                    this.cartCount = data.item_count;
                    this.cartTotalPrice = formatter.format((cartData.total_price/100).toFixed(2));

                    const productArray = this.cartProducts;
                    productArray.forEach((item, index) => {
                        item.price = formatter.format((item.price/100).toFixed(2));
                        item.original_price = formatter.format((item.original_price/100).toFixed(2));
                        item.discounted_price = formatter.format((item.discounted_price/100).toFixed(2));
                        item.line_price = formatter.format((item.line_price/100).toFixed(2));
                        item.original_line_price = formatter.format((item.original_line_price/100).toFixed(2));
                        item.total_discount = formatter.format((item.total_discount/100).toFixed(2));
                        item.final_price = formatter.format((item.final_price/100).toFixed(2));
                        item.final_line_price = formatter.format((item.final_line_price/100).toFixed(2));
                    });

                    const counter = document.getElementById('cart-counter');
                    const counterMb = document.getElementById('cart-counter-mb');
                    counter.textContent = cartData.item_count;
                    counterMb.textContent = cartData.item_count;
                })
                .catch( error => {
                    this.errorMessage = error;
                    console.log('there was an error!', error);
                });

        },
        headerDecrement(index) {
            const headqty = this.cartProducts[index].quantity;
            const headkey = this.cartProducts[index].key;
            const options = {
                method: 'POST',
                headers: { 'content-type': 'application/json' },
                body: JSON.stringify({
                    id: `${headkey}`,
                    quantity: headqty - 1
                })
            };
            fetch('/cart/change.js', options)
                .then( response => {
                    if (response.ok) {
                        return response.json();
                    } else {
                        throw new Error(response.status);
                    }
                })
                .then(data => {
                    
                    const cartData = data;
                    const formatter = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 2, vmaximumFractionDigits: 2, });
                    this.cartProducts = cartData.items;
                    this.cartCount = data.item_count;
                    this.cartTotalPrice = formatter.format((cartData.total_price/100).toFixed(2));

                    const productArray = this.cartProducts;
                    productArray.forEach((item, index) => {
                        item.price = formatter.format((item.price/100).toFixed(2));
                        item.original_price = formatter.format((item.original_price/100).toFixed(2));
                        item.discounted_price = formatter.format((item.discounted_price/100).toFixed(2));
                        item.line_price = formatter.format((item.line_price/100).toFixed(2));
                        item.original_line_price = formatter.format((item.original_line_price/100).toFixed(2));
                        item.total_discount = formatter.format((item.total_discount/100).toFixed(2));
                        item.final_price = formatter.format((item.final_price/100).toFixed(2));
                        item.final_line_price = formatter.format((item.final_line_price/100).toFixed(2));
                    });

                    const counter = document.getElementById('cart-counter');
                    const counterMb = document.getElementById('cart-counter-mb');
                    counter.textContent = cartData.item_count;
                    counterMb.textContent = cartData.item_count;
                })
                .catch( error => {
                    this.errorMessage = error;
                    console.log('there was an error!', error);
                });
        },
        headerRemoveItem(index) {
            const headkey = this.cartProducts[index].key;
            const options = {
                method: 'POST',
                headers: { 'content-type': 'application/json' },
                body: JSON.stringify({
                    id: `${headkey}`,
                    quantity: 0
                })
            };
            fetch('/cart/change.js', options)
                .then( response => {
                    if (response.ok) {
                        return response.json();
                    } else {
                        throw new Error(response.status);
                    }
                })
                .then(data => {
                    
                    const cartData = data;
                    const formatter = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 2, vmaximumFractionDigits: 2, });
                    this.cartProducts = cartData.items;
                    this.cartCount = data.item_count;
                    this.cartTotalPrice = formatter.format((cartData.total_price/100).toFixed(2));

                    const productArray = this.cartProducts;
                    productArray.forEach((item, index) => {
                        item.price = formatter.format((item.price/100).toFixed(2));
                        item.original_price = formatter.format((item.original_price/100).toFixed(2));
                        item.discounted_price = formatter.format((item.discounted_price/100).toFixed(2));
                        item.line_price = formatter.format((item.line_price/100).toFixed(2));
                        item.original_line_price = formatter.format((item.original_line_price/100).toFixed(2));
                        item.total_discount = formatter.format((item.total_discount/100).toFixed(2));
                        item.final_price = formatter.format((item.final_price/100).toFixed(2));
                        item.final_line_price = formatter.format((item.final_line_price/100).toFixed(2));
                    });
                    
                    const counter = document.getElementById('cart-counter');
                    const counterMb = document.getElementById('cart-counter-mb');
                    counter.textContent = cartData.item_count;
                    counterMb.textContent = cartData.item_count;
                })
                .catch( error => {
                    this.errorMessage = error;
                    console.log('there was an error!', error);
                });
        },
        updateCar() {
            const options = {
                method: 'GET'
            }
            setTimeout (() =>{
                fetch('/cart.js', options)
                .then(response => {
                    if(response.ok) {
                        return response.json();
                    } else {
                        throw new Error(response.status);
                    }
                })
                .then(data => {
                    const cartData = data;
                    const formatter = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 2, vmaximumFractionDigits: 2, });
                    this.cartProducts = cartData.items;
                    this.cartCount = data.item_count;
                    this.cartTotalPrice = formatter.format((cartData.total_price/100).toFixed(2));

                    const productArray = this.cartProducts;
                    productArray.forEach((item, index) => {
                        item.price = formatter.format((item.price/100).toFixed(2));
                        item.original_price = formatter.format((item.original_price/100).toFixed(2));
                        item.discounted_price = formatter.format((item.discounted_price/100).toFixed(2));
                        item.line_price = formatter.format((item.line_price/100).toFixed(2));
                        item.original_line_price = formatter.format((item.original_line_price/100).toFixed(2));
                        item.total_discount = formatter.format((item.total_discount/100).toFixed(2));
                        item.final_price = formatter.format((item.final_price/100).toFixed(2));
                        item.final_line_price = formatter.format((item.final_line_price/100).toFixed(2));
                    });
                })
                .catch(error => {
                    console.log(error);
                });
            }, "1000")
        }
    }
});
appHeader.mount('#header')
</script>

<script>
    function myFunctionGrid(id) {
        var x = document.getElementById(id);
        if (x.classList.contains('invisible')) {
            x.classList.remove('invisible');
            x.classList.add('visible');
            x.classList.remove('-left-[480px]');
            x.classList.add('-left-0');
        } else {
            x.classList.remove('visible');
            x.classList.add('invisible');
            x.classList.remove('-left-0');
            x.classList.add('-left-[480px]');
        }
    }
    function myFunctionSearch(id) {
        var x = document.getElementById(id);
        const y = document.getElementById('ext_search');
        if (x.classList.contains('invisible')) {
            x.classList.remove('invisible');
            x.classList.add('visible');
            x.classList.remove('opacity-0');
            x.classList.add('opacity-100');
            y.classList.remove('invisible');
            y.classList.add('visible');
            y.classList.remove('opacity-0');
            y.classList.add('opacity-100');
        } else {
            x.classList.remove('visible');
            x.classList.add('invisible');
            x.classList.remove('opacity-100');
            x.classList.add('opacity-0');
            y.classList.remove('visible');
            y.classList.add('invisible');
            y.classList.remove('opacity-100');
            y.classList.add('opacity-0');
        }
    }  
    function myFunctionMenu(id) {
        var x = document.getElementById(id);
        const y = document.getElementById('int_div');
        const z = document.getElementById('ext_div');
        if (x.classList.contains('invisible')) {
            x.classList.remove('invisible');
            x.classList.add('visible');
            x.classList.remove('opacity-0');
            x.classList.add('opacity-100');
            y.classList.remove('-left-[480px]');
            y.classList.add('-left-0');
            z.classList.remove('invisible');
            z.classList.add('visible');
            z.classList.remove('opacity-0');
            z.classList.add('opacity-100');
        } else {
            x.classList.remove('visible');
            x.classList.add('invisible');
            x.classList.remove('opacity-100');
            x.classList.add('opacity-0');
            y.classList.remove('-left-0');
            y.classList.add('-left-[480px]');
            z.classList.remove('visible');
            z.classList.add('invisible');
            z.classList.remove('opacity-100');
            z.classList.add('opacity-0');
        }
    }
</script>

<script type="text/javascript">
	document.querySelector('.klaviyo_form_trigger1').addEventListener('click', function (){
		window._klOnsite = window._klOnsite || []; 
		window._klOnsite.push(['openForm', 'TvPzMH']);
	});
</script>

{% comment %} <script>
function scrollFunction() {
    document.getElementById("top_banner_mobile").style.visibility = "hidden";
    document.getElementById("top_banner_mobile").style.height = "0px";
    document.getElementById("top_banner_mobile").style.opacity = "0";
}

window.onscroll = scrollFunction;
</script> {% endcomment %}

{% schema %}
{
    "name": "Header",
    "class": "sticky top-0 z-50",
    "settings": [
        {
            "type": "header",
            "content": "Header Bar",
            "info": "Settings for header white bar"
        },
        {
            "type": "url",
            "id": "logo_url",
            "label": "Logo URL"
        },
        {
            "type": "url",
            "id": "custom_url_1",
            "label": "Custom URL 1"
        },
        {
            "type": "text",
            "id": "custom_text_1",
            "label": "Custom text 1"
        },
        {
            "type": "url",
            "id": "custom_url_2",
            "label": "Custom URL 2"
        },
        {
            "type": "text",
            "id": "custom_text_2",
            "label": "Custom text 2"
        },
        {
            "type": "text",
            "id": "button_label",
            "label": "Button Label"
        },
        {
            "type": "url",
            "id": "button_url",
            "label": "Button URL"
        },
        {
            "type": "header",
            "content": "Header Menu",
            "info": "Settings for header menu"
        },
        {
            "type": "link_list",
            "id": "nav_menu",
            "label": "Main menu",
            "default": "main-menu"
        },
        {
            "type": "link_list",
            "id": "bar_menu",
            "label": "Mobile Sub-menu"
        },
        {
            "type": "image_picker",
            "id": "banner",
            "label": "Top Promotional Banner (Desktop)"
        },
        {
            "type": "image_picker",
            "id": "banner_mobile",
            "label": "Top Promotional Banner (Mobile)"
        },
        {
            "type": "url",
            "id": "banner_url",
            "label": "Top Promotional Banner URL"
        },
        {
            "type": "image_picker",
            "id": "banner_1",
            "label": "First Promotional Banner"
        },
        {
            "type": "url",
            "id": "banner_1_url",
            "label": "First Promotional Banner URL"
        },
        {
            "type": "image_picker",
            "id": "banner_2",
            "label": "Second Promotional Banner"
        },
        {
            "type": "url",
            "id": "banner_2_url",
            "label": "Second Promotional Banner URL"
        }
    ]
}
{% endschema %}